# 全名单图片预加载与编队视图

## 图片与视图

- 干员列表只提供「表格 / 半身像」。表格使用头像，半身像使用游戏资源的 `portrait` 竖版图（通常 180×360），不再将全身立绘缩小后塞进卡片。
- 阵容生成的「名单 / 编队」共享同一份生成结果。名单保留原有头像名牌，编队使用与干员列表相同的半身像主体，切换不会重新生成或修改随机池。
- 旧 `Avatar` / `Portrait` 干员列表偏好迁移为 `HalfBody`；出战视图单独保存为 `RosterViewMode`，缺省为 `List`。

## 全量准备，按需创建控件

主界面挂载约 1 秒后，后台遍历整份名单，先准备半身像再准备头像。不要求干员进入可见区域，也不创建离屏卡片控件。

后台与前台共享同 URI 的在途任务、解码位图和缓存。磁盘命中仍需解码，但后台会将解码结果保存在显示缓存中，而不是校验后立即丢弃。已准备好的图片在控件挂载或换源时直接显示，即使正在滚动也不用等待 120ms 的交互静默期。

- 后台单路处理，新图片之间让出至少 25ms；有前台请求或最近 750ms 内发生交互时暂停下一次后台请求。
- 前台最多同时处理 4 个不同图源，解码最多 1 张。没有内存缓存的图片仍在交互静默 120ms 后开始解码。
- 新解码结果按低优先级 16ms 计时器分批显示，每批最多两项、约 2ms 后让出。已命中的内存图片不进入这个启动队列。
- `ArtBitmap` 的图片源只影响绘制，不让图片原始尺寸重新触发布局；占位图通过透明度隐藏。
- 同一候选组按优先级取第一个可用图，不下载每个镜像。缓存记录成功候选，滚动复用不需要重新尝试已经失败的镜像；精英阶段改变则使用新候选组。
- 名单、来源或精英阶段改变时取消旧预加载、去抖建立新快照。移除不再属于任何当前候选组的缓存引用；仍可作回退的旧阶段图在容量不足时优先淘汰，只保护当前组实际使用的图。不释放仍被控件使用的位图。

## 有界内存

桌面显示缓存默认 **2048 张 / 256 MiB**；浏览器默认 **1024 张 / 128 MiB**。上限分别可通过 `ARTIMAGE_CACHE_LIMIT` 和 `ARTIMAGE_CACHE_BYTES` 在启动前配置。预算统计解码像素，不能当作进程内存或 GPU 总占用。

180×360 半身像与 180×180 头像各一张约 380 KiB，334 名干员的两套原生尺寸图片约 124 MiB，不含职业图标、控件、临时缓冲及 GPU 资源。大 PNG 继续限制在 384×640 内等比解码，不放大小图。

后台达到预算后停止扩张，不通过反复淘汰前半份名单来预热后半份名单。此时前台仍按需加载和 LRU 淘汰。「图片加载统计」显示就绪数量、失败状态、预算状态和内存图片用量。首次下载未完成、源图片不存在、预算不足时不能承诺整份名单立即有图。

桌面磁盘缓存仍为默认 **2 GiB / 30 天**，可用 `ARTIMAGE_DISK_BYTES` 调整。磁盘中保存压缩原图；不支持写盘时仍可使用有界的内存预加载。浏览器不写本地磁盘，也没有新增 Cache Storage / IndexedDB，刷新后重新准备。

## 验证方式

使用真实 Windows 桌面程序，不再使用已删除的无头测试工程。从 WSL 构建和启动：

```sh
"/mnt/c/Program Files/dotnet/dotnet.exe" build desktop/arknights-random-team.Desktop.csproj -c Release
./desktop/bin/Release/net10.0/arknights-random-team.exe
```

程序数据与可执行文件放在同一目录。验证时使用真实发布程序的独立副本和现有名单的副本，避免改动原始数据；不要删除用户缓存或锁文件。

重点检查：

1. 图片准备完成后快速滚轮、拖动滚动条、搜索和切换页面，已缓存的半身像立即出现。
2. 统计中图片就绪数量覆盖整份名单；预算较小时显示容量状态，不无限增长。
3. 干员列表只有「表格 / 半身像」，无右上添加干员按钮；池勾选、详情菜单、表格编辑与筛选仍可用。
4. 生成 12 人后切换「名单 / 编队」，人数和姓名保持不变；窄窗口中卡片、名称、等级和工具栏不重叠。
5. 正常退出并重启后，两处视图选择恢复；旧头像/立绘偏好能正常迁移。
6. 断网或图片不存在时保留姓名与职业占位，并提供重试；离树或改源后不显示过期结果。

桌面进程启动成功不等同于交互或帧率验证。Windows 实际窗口截图和交互检查用于本轮验收；浏览器主线程解码和高 DPI / 低内存设备仍需各自实机测量，不把限流机制表述为无条件性能保证。

## 本轮真实程序验证记录

- 使用 Windows .NET 构建 Release 解决方案：桌面与浏览器项目均成功，0 警告、0 错误。实际交互验证在 Windows 桌面可执行程序中完成。
- 334 名干员副本完成 668/668 张图片预加载。最终构建从磁盘预热后显示约 124 MiB 的解码缓存；深滚动前后网络加载均为 0，磁盘命中均为 668，仅内存命中增长。列表末端的可访问卡片为 18 个，控件继续虚拟化。
- 12 人名单切换到编队后，姓名、顺序及人数保持一致；默认窗口采用六列，768px 宽窗口正常换列和滚动。表格、半身像、两处切换控件与窄窗口布局均已通过截图检查。
- 原生鼠标点击入池复选框只改变一次状态，恢复后随机池人数一致；详情编辑、表格显示及旧 Avatar 偏好迁移正常。正常退出和重启后恢复 HalfBody 与 Formation 偏好。
- 单独以 3 名干员、7 张图片上限验证阶段替换：基础阶段为 6/6 就绪，编辑为精二并返回页面后仍为 6/6，总缓存保持 7 张（含职业图标），没有因旧阶段回退图占位而提前停止。
- 验证数据、运行文件和截图位于系统临时目录，未增加测试工程或仓库内的测试文件。
