# 客户端与网页端 UI 美化方案 v1

## 0. 方案定位

**推荐方向：克制的「罗德岛作战终端」——保留明日方舟的识别度，减少装饰，让干员与操作成为主角。**

本方案基于当前工作区的界面、样式及相关逻辑整理，未启动应用进行截图或性能实测。下文区分源码中已有的事实、据此判断的风险，以及建议采用的设计目标；尺寸和颜色是原型起点，不是已验证的最终数值。

本次只整理方案，不修改业务代码或已有未提交改动。

优先解决三件事：

1. **更清爽：** 合并重复标题，减少嵌套边框、彩色底块、光晕和无功能装饰。
2. **更好认、更好管：** 重做干员卡片的信息层级，同时保留表格的编辑效率。
3. **两端真正适配：** 共用设计语言，但不能把固定宽度的桌面界面直接塞进网页小屏。

---

## 1. 现状判断：不是缺少皮肤，而是需要设计收敛

### 已有能力，应继续利用

- 客户端和网页端共用 Avalonia XAML 界面，不需要分别重写两套 UI，见 [共享项目配置](Core/arknights-random-team.Core.csproj#L16-L43)。
- 已有深色主题、薄荷绿主色、按钮状态、职业图标、稀有度配色和内嵌中文字体，见 [全局样式](App.axaml#L9-L67)、[字体配置](AppFonts.cs#L3-L16)。
- 干员列表已有**表格、头像、半身像**三种视图；表格是当前默认视图，已有行内编辑、多列排序、三态全选、随机池计数、删除确认及空状态，见 [列表界面](Views/StaffListView.axaml#L205-L337)、[列表模型](Domain/ListModel.cs#L84-L121)、[列表交互](Views/StaffListView.axaml.cs#L121-L210)。
- 图片已有并发限制、缓存及候选图源回退，不应再把“加头像、加缓存”作为本轮的主要创新点，见 [图片加载实现](Views/ArtImage.cs#L10-L49)。

### 主要问题与改造方向

| 当前实现与风险 | 影响 | 建议 |
| --- | --- | --- |
| 全局标题区高 96，列表内部又出现“干员名单”；侧栏和页面均有中英文字样、压线及几何装饰 | 功能页面的视觉重心容易落在外壳，而非内容 | 保留一个主标题，缩短页头；英文只留在品牌区域，装饰集中到一处。依据：[主框架](Views/MainView.axaml#L311-L384)、[列表工具栏](Views/StaffListView.axaml#L205-L306)。 |
| 列表工具栏在同一横行放置标题、三组统计、图片缓存统计、三种视图和清空按钮，尚无搜索筛选 | 横向空间紧张；面对大量干员缺少定位入口；危险操作过于显眼 | 分清“页面信息、查找、展示方式、危险操作”四类信息。依据：[列表工具栏](Views/StaffListView.axaml#L205-L306)。 |
| 卡片同时叠加稀有度边框与顶栏、职业色、星级、精英角标、编入标签、四角标记和外发光 | 多套颜色和状态争抢注意力，不利于快速认人 | 稀有度、职业、随机池状态各用一种稳定表达，不再重复强化。依据：[卡片模板](Views/StaffListView.axaml#L524-L697)。 |
| 卡片固定宽 180，头像卡高 256、半身像卡高 376，使用普通 WrapPanel | 不同窗口宽度下存在剩余空间和密度问题；控件数量随干员数增长，性能需实测 | 网格按可用宽度计算列数；大立绘作为可选浏览方式，另做虚拟化验证。依据：[卡片尺寸](Views/StaffCardVisual.cs#L11-L46)、[卡片容器](Views/StaffListView.axaml#L508-L518)。 |
| 卡片按 PointerPressed 切换随机池，删除入口仅在悬停时显示 | 触摸滚动可能误触，键盘及无悬停设备的操作入口不充分 | 保留整卡快捷切换，但采用区分点击与拖动的交互；补充键盘和常驻菜单入口。依据：[卡片事件](Views/StaffListView.axaml.cs#L83-L96)、[删除按钮样式](Views/StaffListView.axaml#L102-L117)。 |
| 网页小屏仍强制最小 900px 宽；视口设置禁止缩放 | 不是内容重排式的响应式布局；小屏和放大阅读体验受限 | 在 Avalonia 布局层实现断点，再移除网页最小宽度兜底并恢复缩放。依据：[网页样式](browser/wwwroot/app.css#L30-L38)、[网页视口设置](browser/wwwroot/index.html#L5)。 |
| 共用侧栏显示“本地数据／退出时自动保存”，但网页端实际仅内存保存 | 容易误导用户，属于必须优先修正的状态表达 | 桌面端保留准确的本地保存说明；网页端明确“体验模式，刷新后数据重置”。依据：[侧栏文案](Views/MainView.axaml#L283-L304)、[存储分支](AppState.cs#L25-L29)。 |

---

## 2. 统一视觉规范

### 2.1 风格原则

- **暗色、平面、低饱和。** 保留现有薄荷绿，不再叠加更多霓虹色。
- **方舟气质来自排版、职业图标和干员图像。** 不依靠每个区域都画斜线、角标和灯带。
- **一个区域一个重点。** 正常页面只保留一个主要实心操作按钮；其余使用次级或文本按钮。
- **装饰做减法，状态做加法。** 减少无信息的边框，却补足悬停、焦点、禁用、失败等状态。

### 2.2 建议的设计参数

除网页容器说明外，布局尺寸均指 Avalonia 逻辑像素，应结合系统缩放验证。

| 项目 | 建议基线 |
| --- | --- |
| 背景层级 | 页面 `#101418` → 面板 `#181E24` → 悬停 `#202831`；先以中性灰拉开层次 |
| 描边 | `#303A45`，普通面板最多一层细描边，避免“框里套框” |
| 主色 | 沿用 `#65E6C4`，仅用于主操作、当前导航、已加入随机池等重点 |
| 文本 | 主文字 `#EDF4FA`；次文字建议 `#A5B2BF`，以实际对比度测试为准 |
| 语义色 | 稀有度保留现有映射；红色仅用于危险与错误，黄色不再作为通用装饰 |
| 字级 | 页面标题 24；分区标题 18；干员名 15–16；正文 14；辅助信息 12 |
| 字体 | 优先复用现有中文字体统一两端观感；等宽字体仅用于数字与短编号，不用于大段中文 |
| 间距 | 统一为 4／8／12／16／24；桌面页面内边距 24，小屏 16 |
| 圆角 | 控件 6，卡片／面板 8，弹层 12；减少同一层级混用多种圆角 |
| 控件高度 | 桌面主要输入与按钮 40；触摸主操作命中区域不小于 44×44 |
| 动效 | 120–180ms 的颜色或透明度过渡；列表不做持续发光、逐卡飞入或大幅缩放 |

**不要只换色值。** 同步清理页面中写死的颜色和重复样式，否则只是另一套颜色的旧布局。普通边界可以低对比，交互控件边界与焦点提示必须足够清晰。

---

## 3. 干员列表页：本轮核心

### 3.1 页面结构

推荐的桌面宽屏最终布局如下，数字均为示意；搜索、筛选等新增能力放在第二阶段，不混入纯视觉工作量。

```text
侧栏    干员列表   共 320 名 · 已加入随机池 128 名       [添加干员 ▾] [⋯]
        ──────────────────────────────────────────────────────────
        [搜索干员名称……              ] [筛选] [排序] [头像 ▾]
        职业：近卫   稀有度：六星   随机池：全部
        已筛选：近卫 ×  六星 ×                         [清除筛选]

        显示 18 / 320 名
        ┌───────────┐ ┌───────────┐ ┌───────────┐ …
        │6★    □入池│ │6★    ✓入池│ │6★    □入池│
        │   干员图像 │ │   干员图像 │ │   干员图像 │
        ├───────────┤ ├───────────┤ ├───────────┤
        │干员名称   ⋯│ │干员名称   ⋯│ │干员名称   ⋯│
        │近卫 精二90 │ │近卫 精二90 │ │近卫 精一70 │
        └───────────┘ └───────────┘ └───────────┘

        [将当前筛选结果加入随机池] [将当前筛选结果移出随机池]
```

布局规则：

- 合并外壳标题和页面内重复标题；计数改成轻量摘要，不再为每个数字建一张小卡。
- “添加干员”提供手动录入／数据库同步入口，复用现有流程，不再维护第二套录入逻辑。
- “清空列表”放入更多菜单，保留现有二次确认，并明确影响全部干员，不受筛选限制。
- 图片网络次数、内存命中数等移入诊断信息；普通页面只在必要时提示“部分图片加载失败”。
- 工具区与列表滚动区分离，不随几百名干员一起滚走；仅在有筛选条件时显示已选条件行。

### 3.2 保留三种视图，明确各自用途

| 视图 | 定位 | 调整重点 |
| --- | --- | --- |
| 头像 | 日常认人、快速维护随机池，作为视觉设计主样板 | 紧凑、清晰、稳定的自适应网格 |
| 表格 | 修改等级、稀有度、职业及大量数据管理 | 保留行内编辑、多列排序和三态全选，降低装饰密度 |
| 立绘 | 欣赏与浏览，而非最高效的管理界面 | 将“半身像”改为与图源更吻合的“立绘”；大图不挤占默认管理空间 |

**首期不强行更改默认表格。** 等卡片编辑入口、键盘支持与性能验证完成后，再评估是否让新用户默认进入头像视图；已有用户的选择应优先。视图偏好记忆是独立增强，不等于网页端已经支持干员数据持久化。

### 3.3 卡片设计

#### 建议的信息顺序

1. **干员图像与名称：** 第一眼先认出是谁。
2. **是否在随机池：** 使用明确勾选标记及“入池”文案，不仅依赖颜色。
3. **职业、精英阶段、等级：** 位于稳定的信息区，名称不与状态标签争抢宽度。
4. **稀有度：** 用小面积星级／短标记表达，不再让整张卡的边框和彩色条都承担同一语义。

#### 建议样式

- 头像卡单元宽度约 **156–196**，间距 **12**，根据内容区域宽度计算列数；名牌约 **60** 高。尺寸随原型验证调整，不再固定每张 180 后留下大块尾部空白。
- 使用统一的浅中性色边框；选中时使用一圈主色边框加勾选标记，去掉四角标记和持续外发光。
- 未加入随机池的干员仍保持图像和名称可读，不让“未入池”看起来像不可用或未拥有。
- 职业以官方图标和文字识别，取消同屏大面积职业彩色底块；职业色仅在必要的小标记中保留。
- 名称单行省略，桌面悬停可看全名；触摸端可通过详情查看，不依赖 tooltip。
- 使用“精二 · Lv.90”等易读格式，避免“精二”和“2-90”同时出现的重复表达。
- 普通卡片不放红色删除按钮；改为可发现、可聚焦的更多菜单。触摸端菜单常驻，不能只在 hover 时出现。
- 保持边框占位尺寸稳定，通过颜色而非边框粗细变化表达 hover，避免卡片内容轻微位移。

#### 图片规则

- 头像优先使用现有头像资源；立绘采用独立裁剪规则，不能把同一个 `UniformToFill` 当作所有图源的答案。
- 用不同身形、透明边距、长武器及精二构图的代表干员做对照，先保证脸和主体不被不合理裁切。
- 固定图片区和名牌高度，图片到达前后不引发布局跳动。
- 区分“尚未加载／加载成功／加载失败／没有图源”；不能把“存在候选 URL”当作加载成功。
- 图片失败时保留职业图标、名称和可用操作。先复用现有图源与内嵌图标，不为美化打包全量高清立绘或新增大背景图。

### 3.4 表格设计

- 建议列顺序：**加入随机池｜干员｜稀有度｜职业｜精英／等级｜更多**。
- 保留干员列中的小头像和名称；若职业保留为独立列，就删掉名称下面重复的职业文字。
- 普通行高建议 **56**，紧凑模式建议 **44**；对应头像约 40／32，不再每个字段都套描边徽章。
- 名称左对齐，数字格式一致；弱化网格线和斑马纹，保留清晰的表头、hover 与键盘焦点。
- 保留现有行内编辑，不把全部字段常驻显示为输入框；补充轻量编辑提示，让用户知道哪些内容可改。
- 排序状态显示箭头，必要时显示多列优先级；**行焦点、正在编辑、加入随机池**必须是三种不同状态。保留现有“升序 → 降序 → 移除该排序”的循环与多列优先级，见 [多列排序实现](Views/DataGridMultiSort.cs#L9-L28)。
- 展示格式和编辑输入分离，不能直接改变等级文案后破坏现有解析。后续可将等级编辑改为“精英阶段＋数值等级”，并就地提示无效输入；现有解析失败会直接返回，见 [等级模型](Models/Level.cs#L34-L46)。
- 窄屏优先头像或紧凑条目；用户主动选择表格时，横向滚动限于表格区域，不让整个网页横向移动。

### 3.5 搜索、筛选与批量操作——单独计入交互增强

建议最小集合：

- 搜索：先支持名称包含匹配，不在首版承诺拼音、模糊匹配和复杂查询。
- 筛选：职业、稀有度、随机池状态；同组多选为“或”，不同组之间为“且”。
- 排序：名称、稀有度、职业、等级；卡片和表格共享同一份结果及排序状态。等级按精英阶段与等级数值比较，不按显示字符串排序；这是需单独验证的行为调整。
- 批量：优先提供“将当前筛选结果加入／移出随机池”，**当前结果指所有匹配项，不是当前屏幕已加载的卡片**。小屏将长按钮收进带匹配数量的批量操作菜单，避免底栏挤占列表。
- 三态全选在有筛选时必须标明作用范围；新增过滤不能悄悄更改隐藏干员的随机池状态。
- 保留源集合与筛选结果的分离，筛选、排序只是展示投影，不删除干员、不改变随机算法的输入语义。

如果后续加入“批量删除”，必须单独维护“批量操作选中的项目”，不能复用目前表示“加入随机池”的 `IsSelected`。首版不做复杂批量编辑器。

### 3.6 交互与状态补齐

- 保留整卡点击加入／移出随机池的快捷操作，但不能在指针刚按下时就提交；应区分主键点击、触摸拖动和菜单操作。
- 卡片操作使用可聚焦控件，支持 Tab、Space／Enter，补足名称、勾选状态等无障碍语义。
- 编辑由明确入口触发；后续桌面可采用侧栏编辑，小屏采用全屏编辑面板，避免点击整卡同时承担选池和打开详情两种含义。
- “暂无干员”提供录入／同步入口；“筛选无结果”提供清除筛选；图片失败不能把整个列表表现为不可用。
- 空状态和提示沿用统一文字与按钮样式，不引入大幅占位插画。

---

## 4. 其他页面的配套改造

| 页面 | 推荐调整 | 保留与边界 |
| --- | --- | --- |
| 主框架 | 桌面页头由 96 压缩至约 64；导航采用功能图标＋中文，弱化编号和英文副标题；删除重复灯带与装饰线 | 保留四个现有功能入口，不为美化重新设计整套导航流程。对应：[主框架](Views/MainView.axaml)。 |
| 阵容生成 | 压缩配置区和重复口号，将“策略、人数、生成按钮”组织为清晰操作区；给结果卡片更多空间，共享干员视觉组件 | 保留人数预设、滑块和现有生成逻辑；不增加无必要的抽卡动画。对应：[生成页](Views/GenerateView.axaml#L198-L425)。 |
| 干员录入 | 表单字段统一字级、间距和控件高度；数据库同步作为次级区块；窄屏由双栏变上下排列 | 保留现有校验与同步流程。字段错误靠近字段显示，不只改变颜色。对应：[录入页](Views/InputView.axaml#L54-L193)。 |
| 随机策略 | 名称＋少量规则摘要形成轻量卡片；较多规则按需展开；编辑为次级操作，删除进入更多菜单 | 保留原有规则含义及编辑能力，不把每条规则继续包进多层面板。对应：[策略页](Views/RandomStrategyView.axaml#L72-L131)。 |
| 弹窗 | 标题、正文、底部操作区统一；主次按钮一致；长内容滚动时底部动作可达 | 桌面保留原生模态窗口，网页保留叠加层；统一的是内容与交互，不强行统一窗口机制。对应：[桌面弹窗宿主](Views/WindowPresenter.cs#L9-L39)、[网页弹窗宿主](Views/OverlayPresenter.cs#L12-L67)。 |
| 网页首屏 | 沿用现有加载遮罩，简化标识；补充运行时启动失败说明与重试入口；应用真正可用后再移除遮罩 | 没有真实下载进度时显示阶段文字，不伪造百分比。对应：[网页入口](browser/wwwroot/index.html#L10-L64)、[启动脚本](browser/wwwroot/main.js#L7-L14)。 |

---

## 5. 客户端与网页端适配

### 5.1 断点建议

以下按应用可用逻辑宽度划分，列数仍由实际内容宽度计算，不靠设备名称判断。

| 可用宽度 | 导航与页头 | 内容区 |
| --- | --- | --- |
| ≥1200 | 约 208 宽完整侧栏，64 高页头 | 搜索、筛选、视图切换可以并排；头像网格自然排布 |
| 900–1199 | 约 72 宽图标导航，可展开 | 优先保障干员内容宽度，工具栏必要时换行 |
| 600–899 | 顶部导航入口＋抽屉 | 筛选进入展开面板；录入表单和编辑器改单栏 |
| <600 | 约 56 高紧凑页头＋抽屉 | 390 宽视口优先两列头像；不足以容纳两列最小卡宽时改单列紧凑条目；复杂编辑全屏展示 |

### 5.2 实施注意

- 内部布局调整发生在 Avalonia 中；网页 CSS 只能控制外层容器和加载页，不能替代干员卡片的 XAML 改造。
- **先完成小宽度布局，再撤销 900px 最小宽度。** 只删 CSS 限制会把现有布局压坏。
- 桌面端先覆盖当前默认和最小窗口，是否降低现有最小窗口尺寸另行验证，见 [桌面窗口配置](MainWindow.axaml#L5-L9)。
- 网页恢复用户缩放能力，并验证字体、浏览器缩放、触摸键盘出现时的可视区域及弹层底部按钮。
- 两端统一语义、组件和状态，但保存提示必须按平台分支；本轮不把网页持久化当作已实现能力。

### 5.3 流畅度也是美观的一部分

- 现有普通 WrapPanel 不提供卡片虚拟化；图片缓存也不等于列表虚拟化。
- 先用代表性干员数量测量布局、滚动、内存和请求量，再验证与当前 Avalonia 版本兼容的虚拟化方案；必要时采用虚拟化行容器承载网格，不假设现成控件可直接替换。
- 首屏优先当前视图的可见图片，预取仅覆盖邻近区域；保留缓存、限流和顺序回退，不让所有卡片同时预载另一规格的所有候选 URL。依据：[当前预载逻辑](Views/ArtImage.cs#L100-L143)。
- 不因加入更多光效、大立绘和骨架动画而提高 WASM 首屏负担。失败时先保证文字和交互可用。

---

## 6. 工程落点

| 改造模块 | 主要落点 | 注意事项 |
| --- | --- | --- |
| 设计参数、公共控件 | [全局样式](App.axaml)、[展示派生属性](Models/Staff.Visual.cs) | 收敛同义色值、间距、圆角、字级；复用现有中文字体，保持呈现属性与存储字段分离 |
| 导航、页头、平台提示 | [主框架](Views/MainView.axaml)、[主框架逻辑](Views/MainView.axaml.cs) | 建立可复用的紧凑／宽屏状态，不在每页单独猜测设备 |
| 卡片、表格、操作区 | [干员列表界面](Views/StaffListView.axaml)、[卡片尺寸映射](Views/StaffCardVisual.cs) | 先提取可复用卡片视觉，再同步生成页，避免复制两套模板后继续分化 |
| 搜索、筛选、排序、操作范围 | [列表模型](Domain/ListModel.cs)、[列表交互](Views/StaffListView.axaml.cs) | 新增展示投影；保留原数据与随机池语义；不要借机改随机算法 |
| 图片状态与加载调度 | [图片加载实现](Views/ArtImage.cs)、[展示派生属性](Models/Staff.Visual.cs) | 区分图源存在与加载成功，兼容现有缓存和回退；性能修改独立验证 |
| 网页外层与加载体验 | [网页样式](browser/wwwroot/app.css)、[网页入口](browser/wwwroot/index.html)、[启动脚本](browser/wwwroot/main.js) | 处理容器尺寸、缩放、首屏状态，不尝试靠 CSS 重写画布内 UI |
| 配套页面 | [生成页](Views/GenerateView.axaml)、[录入页](Views/InputView.axaml)、[策略页](Views/RandomStrategyView.axaml) | 干员页样板确认后推广，不同时重排全部页面导致风格反复 |

---

## 7. 实施顺序与范围控制

| 阶段 | 工作内容 | 完成门槛 |
| --- | --- | --- |
| 设计确认 | 对现有界面留存同数据截图；制作干员页桌面／窄屏样板，展示头像、表格、选池状态和缺图状态 | 确认视觉方向后再编码；不要一开始同时尝试多套主题 |
| **P0：视觉收敛** | 统一参数，精简外壳和列表标题，重排计数，清理卡片装饰，调整表格层级，弱化危险操作，修正网页保存提示 | 客户端默认／最小窗口及宽屏网页外观一致；三种视图、编辑、全选、排序、删除确认行为不退化 |
| **P1：查找与适配** | 增加名称搜索、基础筛选、卡片排序、明确的批量入池范围；补齐触摸与键盘；实现窄屏重排、网页缩放和图片失败状态；完成必要的性能优化 | 390 宽等窄屏可完成核心操作，500 名测试干员下完成性能和状态回归 |
| **P2：全站推广与打磨** | 将规范推广到生成、录入、策略及弹窗；补足网页启动错误体验；按测量结果继续优化图片调度和视图偏好 | 两端截图、交互与性能检查通过；不留独立风格的旧页面 |

其中 P1 的搜索、筛选、批量和编辑面板属于新增交互能力，虚拟化属于工程优化；都应独立估算，不能按“改几个样式”计入成本。若只做一轮，优先交付 **P0 干员列表完整样板＋网页真实状态提示**。

首轮不做：浅色主题、多个皮肤切换、全新 Web 技术栈、网页数据持久化、账户系统、全量高清资源包、随机算法重写。这些都不是解决当前美观问题的必要条件。

---

## 8. 验收清单

### 视觉与布局

- 同一批干员、相同随机池状态、相同窗口尺寸截图对比，不能只看空列表或少量漂亮立绘。
- 客户端覆盖 1160×820、960×700，并验证 100%／150%／200% 系统缩放；网页覆盖 1440×900、1024×768、768×1024、390×844，补测浏览器放大。
- 核心操作无截断、无覆盖；正文不依赖 9–10px 小字；每个页面仅保留一个主标题。
- 普通文本对比度以 4.5:1 为目标，大字及关键控件／焦点提示以相应的 3:1 要求核验；稀有度和选池状态不只靠颜色区分。
- 无悬停设备也能编辑、删除和查看完整名称；模态框打开、关闭和焦点恢复行为正常。

### 数据与交互

- 覆盖 0／1／50／500 名干员、长名称、无图源、图片加载失败、不同精英阶段及不同稀有度。
- 表格与卡片切换不丢失随机池状态；排序、编辑与行焦点不能意外切换入池状态。
- 触摸滚动及右键不误选；点击更多菜单不触发整卡选池；键盘能完成主要操作。
- 筛选后的批量入池只影响所有匹配项；清空列表仍明确影响全部项目；取消危险操作不修改数据。
- 新增入口整合后，保留手动录入默认入池、数据库同步新增默认不入池的差异，见 [手动录入逻辑](Views/InputView.axaml.cs#L137-L144)、[同步新增逻辑](Domain/OperatorSyncService.cs#L144-L152)。
- 精英阶段改变后图像与标签同步更新；快速切换视图、滚动回收和图片失败重试时不串图、不释放仍在显示的位图。
- 网页提示与实际存储行为一致，不出现“已保存到本地”的错误承诺。

### 性能目标（待建立基线后验证）

- 记录测试设备、平台、数据量、网络条件及冷／热缓存，分别测试桌面和 WASM，不凭肉眼宣称性能已解决。
- 以 500 名干员为设计测试集，名称输入停止后结果更新目标不超过 200ms；普通选池操作在 100ms 内出现本地视觉反馈。
- 列表滚动以接近 60fps 为优化目标，记录连续卡顿及内存变化；未达到时先削减装饰和图片负载，而非隐藏数据。
- 图片请求量与当前视图和附近可见区域相关，不因切入头像模式就拉取全部立绘候选；远端失败不阻塞文字和随机池操作。

**最终目标：保留方舟味，但从“装饰较多的工具界面”变成“认人迅速、操作明确、两端一致的干员管理界面”。**
