# UI 与性能优化方案 v2：实施结果与后续边界

## 1. 当前结论

本轮不再继续堆叠状态机、缓存账本或恢复副本。现有视觉方向、响应式布局和虚拟化方案保留；代码已经按以下原则收敛：

- 数据文件只保留一套原子写入、一个固定 `.bak` 和必要的坏文件隔离。
- 同一数据目录只允许一个可写桌面实例；无法取得目录锁时以只读模式启动。
- 加载先完成整份快照校验，再替换内存集合，禁止半份数据进入运行态。
- 退出只保存内容确实变化的文件；保存失败可取消退出，不再静默丢失本次修改。
- 图片层分开处理共享在途任务、URI 失败缓存和当前控件失败状态。
- 页面和全局事件按可视树挂载期订阅，离开页面即退订。
- 表格、卡片和弹层只保留一套输入/焦点生命周期，不再叠加重复事件路径。

## 2. 已删除的过度设计

此前为了覆盖极端恢复场景引入的下列机制已经移除：

- rescue 文件、备份哈希、consumed 标记及其清理状态机；
- 图片失败重试字典与近似磁盘字节账本；
- 同一交互在 XAML 元素和根视图上的两套指针事件；
- 切换头像/立绘时对另一规格进行全量预取；
- 无调用方的视觉属性、计数字段和 XAML 名称；
- 无变化退出时仍重写全部文件并轮换 `.bak` 的行为。

保留下来的保护都有直接的数据安全或用户可见用途：原子替换、单份备份、坏文件留证、不可稳定读取时禁止覆盖，以及保存失败提示。

## 3. 数据持久化

### 3.1 写入契约

`FileStore` 的写入流程固定为：

1. 写唯一临时文件；
2. 与当前主文件逐字节比较，相同则跳过；
3. 不同时用原子替换提交，并把上一版保留为 `.bak`；
4. 清理本次临时文件。

应用层在初始化后保存四份规范化内容快照。退出时重新序列化并与快照比较，只提交有变化的文件。这样既避免无意义 IO，也不会把唯一的历史备份覆盖成与主文件相同的副本。

不再额外维护外部文件哈希或版本账本：协作进程由同目录锁保证单写；若非协作工具在运行期间改写文件，应用提交时的当前磁盘版本仍会由原子替换保留为 `.bak`。应用自身没有修改时则不写回，也不主动重建被外部删除的文件。

### 3.2 读取契约

每个文件只有四种读取结果：

- `Missing`：目标文件确实不存在；
- `Success`：整份内容解析和模型校验完成；
- `Invalid`：文件可读，但结构或值域无效；
- `Unavailable`：权限、占用或 IO 状态使内容无法稳定读取。

只有 `Invalid` 才会进入隔离/备份恢复；`Unavailable` 不改磁盘并阻止本会话写回。若无现有备份且坏主文件只是暂时无法移动，允许之后尝试原子保存，旧坏内容会成为 `.bak`，不再把整场修改永久冻结。

### 3.3 模型边界

当前加载和保存边界会拒绝：

- 未定义的 `Career` / `StaffViewMode` / 策略规则枚举值；
- 未知或重复的 Staff XML 节点，以及重复的干员名称或非空来源 ID；
- 存在但不可解析的星级、选择状态和等级；
- XML 不允许的控制字符；
- null 策略 DTO / 规则；
- 超出 `AppOptions.MaxTeamSize` 的策略人数或范围；
- 无法通过整份策略冲突校验的数据。

解析始终先写临时集合，全部成功后才替换全局集合。

## 4. 列表与交互

- 批量更新期间只记录待刷新状态，事务结束后统一更新投影和计数。
- `ListModel` 只在页面激活期订阅全局集合和干员属性，Reset 时会完整清理旧订阅。
- 多列排序使用独立的选择器哨兵值；选择“默认排序”可以真正清空多列排序。
- 表格内联名称在提交前统一 `Trim`，重名比较也使用规范化名称。
- 卡片和表格单元格共享根级指针跟踪：只有同一指针、同一目标、未越过拖动阈值的释放才提交。
- 复选框、菜单按钮和拖动不会触发整卡的随机池切换。
- 键盘表头排序完成后会恢复列头焦点并清理临时行选择抑制状态。

## 5. 图片与磁盘缓存

图片加载当前分为三类状态：

- `LoadEntry`：每个 URI 的共享在途任务，记录有效等待者；最后一个等待者离开时取消底层工作；
- `FailedCache`：有界的 URI 负缓存，避免重复请求同一失败地址；
- `FailedImages`：当前仍处于失败状态的图片控件，用于界面横幅，不与历史失败次数混用。

关键约束：

- 本地资源和磁盘命中不占网络并发名额；
- HTTP、读流和磁盘读取都接受取消令牌；
- 本地缓存解码失败会删除坏字节并回源一次；
- 网络内容只有成功解码后才落盘；
- 回写 UI 前同时校验图源对象、所属加载轮次和真实可视树状态；
- 成功位图按估算像素字节和条数共同限制；
- 解码单并发，滚动活跃时暂停下一次解码及图片 UI 提交；空闲 120ms 后用低优先级 16ms 计时器分批处理，每批最多两项，统计通知节流到 250ms；
- 主图与占位图的 `ArtBitmap.Source` 只影响绘制；占位在成功时改变透明度，不重新折叠布局。PNG 按最大 384×640 等比解码；
- 磁盘缓存按默认 2 GiB、30 天和临时文件年龄定期扫描，可用 `ARTIMAGE_DISK_BYTES` 调整预算；不维护容易漂移的常驻字节账本。
- 桌面端新增全列表头像/立绘单路后台预取，前台加载及交互优先；压缩字节落盘，校验位图立即释放，不占显示 LRU。实现及平台边界见 [图片预取说明](IMAGE-PREFETCH.md)。

## 6. 弹层与小屏

- 每层弹窗有自己的遮罩，只有栈顶可以命中和接收 Tab。
- 取消令牌回调统一调度到 UI 线程，并在弹层加入栈后才注册。
- 子弹层关闭后先恢复父层命中与尺寸；原焦点无法恢复时才聚焦父层首个控件。
- 预取消请求在修改对话框尺寸或逻辑父级之前直接返回。
- 手机复杂编辑器使用全屏布局；正文滚动、页脚固定，背景使用不透明表面色。

## 7. 验证结果

本轮实际执行：

- Release 全解决方案构建：0 警告、0 错误；
- 完整 Avalonia Headless 冒烟：桌面、平板、390px 手机布局全部完成，无绑定错误；
- 交互链路：卡片轻点、拖动抑制、更多菜单、表格单元格、复选框、键盘排序、批量筛选与嵌套弹层均通过；
- 500 名数据：卡片视图只实例化 29 个控件，虚拟化保持有效；
- 图片失败与手动重试路径通过；
- 持久化恢复矩阵 15/15 通过，覆盖主/备份损坏、文件占用、无备份隔离失败、只读模式、非法模型值、重复身份和原子备份恢复；干员重命名引用迁移、同步名称冲突与复合等级单次投影通知探针也通过；
- 390×844 的详情与策略编辑器截图确认全屏布局没有底层内容泄漏或页脚遮挡。

NuGet 漏洞元数据查询曾因网络不可达产生 `NU1900`，不影响构建和冒烟结果。

## 8. 有意保留的后续验证

以下内容没有用源码推断冒充实测：

- 真实 Windows GPU 帧时间和长时间滚动内存曲线；
- 实际 WASM 浏览器的冷启动字节、断网恢复和软键盘动态视口；
- 真机多点触控取消手势；
- 读屏器与完整无障碍名称覆盖。

这些应作为独立测量任务处理。当前不再为了尚未测出的瓶颈增加调度器、后台保存队列、缓存索引或新的恢复状态机。
