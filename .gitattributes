# 统一行尾：仓库与工作区都按 LF。
# 为什么必须写 eol=lf：Windows 侧 core.autocrlf=true 会在检出一律给出 CRLF，
# 结果是 git status 长期挂着几十个“只差行尾”的假修改，脚本做多行匹配也会失配。
* text=auto eol=lf

# 二进制与生成物不做行尾转换
*.png binary
*.otf binary
*.ico binary
*.dll binary
*.pdb binary
*.snap binary

# 脚本固定 LF，避免在 Linux/macOS 上出现 shebang 解析问题
*.sh text eol=lf
*.py text eol=lf

# Windows 专用文件保留 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# .sln 在仓库里的历史版本本身就是 CRLF，交给 Visual Studio 维护即可。
# 这里显式关闭行尾转换，避免出现「只改了行尾」的假差异。
*.sln -text
