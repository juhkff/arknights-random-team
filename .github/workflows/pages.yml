name: Deploy Web (GitHub Pages)

on:
  # github-pages 环境禁止从标签部署。标签推送会先跑「Build and Publish」，
  # 成功后再由这里以默认分支身份领取同一提交并发布，这样环境规则能通过。
  workflow_run:
    workflows: ["Build and Publish"]
    types: [completed]
  workflow_dispatch:
  # 仅在本工作流自身变更时部署一次，方便修好发布通道；日常提交不会走这里。
  push:
    branches: ["master"]
    paths:
      - ".github/workflows/pages.yml"

permissions:
  contents: read
  pages: write
  id-token: write

# 同一时间只保留一次部署；新的推送取消排队中的旧任务，但不打断正在进行的发布。
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    name: Build WebAssembly
    if: ${{ github.event_name != 'workflow_run' || (github.event.workflow_run.conclusion == 'success' && startsWith(github.event.workflow_run.head_branch, 'v')) }}
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: "10.0.x"

      # 显式缓存 NuGet 包目录（wasm-tools 与运行时包都在其中，省下的时间很可观）
      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}
          restore-keys: ${{ runner.os }}-nuget-

      - name: Install wasm-tools workload
        run: dotnet workload install wasm-tools

      - name: Publish WebAssembly
        run: >-
          dotnet publish browser/arknights-random-team.Browser.csproj
          --configuration Release
          --output publish/web

      - name: Prepare site
        run: |
          # 静态站点根目录就是 wwwroot；同时写入 .nojekyll，
          # 避免 GitHub Pages 的 Jekyll 处理把 _framework 目录忽略掉。
          mkdir -p site
          cp -r publish/web/wwwroot/. site/
          touch site/.nojekyll
          du -sh site
          test -f site/index.html
          test -f site/main.js
          test -f site/_framework/dotnet.js

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    name: Deploy
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
